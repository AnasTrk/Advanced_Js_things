//SOME NODDE JS 
    //MODULES
//callbacks    
/*var value = ()=>{
    return 'wroks like magic';
}
function  ano (callback) {
    return callback;
}
console.log(ano(value()));

//ASYNC & SYNC FUNCTIONS
let async = (realTiming)=>{
    console.log(`executed after ...${realTiming}`);
}
var time = 3000;
setInterval((time)=>{
    setTimeout((time) => {
        var date =new Date();
        async(date.getSeconds());
    }, time);
},time);*/
//ajax HttpRequests
//FIRE BASE
